# Shared rocky-coast-labs Supabase project — server-side only, no anon key needed
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Anthropic Claude API — chat + email-scan extraction
ANTHROPIC_API_KEY=

# Gmail OAuth (single-mailbox, read-only) — set up in Phase 6
GMAIL_OAUTH_CLIENT_ID=
GMAIL_OAUTH_CLIENT_SECRET=

# Shared secret for the email-scan cron trigger route
CRON_SECRET=
